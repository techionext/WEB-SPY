export const APP_NAME = "template-name";

export const appConfig = {
  name: APP_NAME,
  token: `${APP_NAME}-token`,
  title: `${APP_NAME}`,
  description: `${APP_NAME}`,
};

export const tokenExpired = 0.3333;
