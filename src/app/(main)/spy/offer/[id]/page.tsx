import { ScreenSpyOfferId } from "@/screens/spy/offer-id";
import { Suspense } from "react";

export default function SpyOfferIdPage() {
  return (
    <Suspense>
      <ScreenSpyOfferId />
    </Suspense>
  );
}
